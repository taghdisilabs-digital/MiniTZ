# Biella Engine — Durable Project Context Snapshot

Date: 2026-08-27
Status: CURRENT DURABLE CONTEXT SNAPSHOT

## Purpose

Compact durable context for future Codex/ChatGPT sessions. Current repository/runtime state remains authoritative for implementation truth; Google Drive remains the live project-document source.

## Authority and identity

Biella Engine is a project-neutral universal AI production/execution engine. Mahdi Taghdisi is the final authority for product direction, priorities, architecture, infrastructure use, acceptance, and work policy.

Legacy projects are donor sources only. Preserve productive software, code, technical knowledge, production pipelines, tooling, execution/recovery logic, benchmarks/failure lessons, and useful agent/specialist patterns. Do not inherit legacy project identity, branding/lore, fixed hierarchy, blocker/governance rules, hard-QA stages, artificial limits, stale infrastructure/provider assumptions, or nonproductive control systems.

## No artificial blockers

Numbered prompts and phases are technical work/navigation units, not mandatory stop/approval gates. Productive donor analysis and Biella implementation may proceed whenever useful and dependency-safe. Validation is proportional to the concrete result being claimed; no universal maker/critic/validator/repair chain, readiness ceremony, fixed repair count, qualification period, or global reviewer requirement exists.

## Durable kernel

Project, Task, Run, Capability, Graph, Node, Artifact, Resource, Event, Knowledge.

Project facts remain Project-scoped. Hardware is Resource state. Providers/models/tools/runtimes are replaceable implementations. Graph revisions are immutable; replanning creates a new revision. Independent Nodes may run concurrently when dependencies, side effects, and Resources permit.

Artifact != ContentRef != StorageLocation. Keep Engine Memory, Project Memory, Run Memory, Historical Evidence, and Cache separate. Cache is rebuildable and non-authoritative.

## Productive legacy reuse

Productive donor material may be reused directly when already clean and compatible, or adapted when project coupling must be removed. Classification, quarantine, provenance, or migration helper types are optional mechanisms when they materially help safety, identity, debugging, deduplication, recovery, or integrity; they are not universal prerequisites for productive reuse.

## Current durable repository

Repository: `patrickminitz-web/biella-engine`
Branch: `main`

At the time this snapshot was refreshed, the remote repository remained documentation/operations-heavy and did not yet prove a durable Engine implementation solely from old historical reports. Current code truth must be re-read from the actual repository/worktree before implementation claims are made.

A previously observed local implementation and test result may remain useful historical evidence, but missing source bytes are not reconstructed from prose. Reuse real surviving code when available; otherwise implement from current requirements and source.

## 51-prompt program role

The P0-P4 program remains a technical work map:

- P0: core contracts and productive legacy reuse
- P1: durable cognition, resources, scheduling, routing
- P2: universal execution adapters
- P3: production capability packs
- P4: evidence-based learning and reusable production intelligence

P0-01 is **Legacy Productive Reuse**. The program does not impose universal phase gates or a mandatory final qualification ceremony. Continue dependency-safe productive work according to the current owner objective.

## Build discipline

Inspect task-relevant current state -> execute the smallest useful correct change -> run focused evidence appropriate to the claim -> persist meaningful source/artifacts/state -> continue.

Do not claim working, tested, built, deployed, published, or verified results unless actually observed.
