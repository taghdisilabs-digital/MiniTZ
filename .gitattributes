# Raw Unreal evidence is immutable. Preserve exact bytes and exclude only
# producer-owned logs/traces from source whitespace checks, at every task path.
projects/biella-games/Build/**/*.log -text -whitespace
projects/biella-games/Build/**/*.csv -text -whitespace
projects/biella-games/Build/**/*.txt -text -whitespace
