# D16-03 Task Guide — Qualify clean-machine install and launch

```yaml
schema: biella.games_task_guide/v1
task_id: D16-03
project: Biella Games
status: PREPARED_TASK_INPUT
priority: GAME_FIRST
execution_root: projects/biella-games
```

## Objective
Install or unpack the package on a machine/environment that does not depend on the development editor workspace and prove clean launch.

## Exact inputs
- `Build/Release/D08-01/qualification.json`
- `Build/Release/D08-01/D08-01-resource-observation.json`
- `tests/run_d01_048.py`
- `tests/run_d03_01_package.py`

D08 output paths above are the authorized predecessor outputs, not a claim that they already exist. Resolve them after accepted D08 closure. If a required record is missing, repair that exact handoff rather than searching all Drive/history or fabricating it. Reuse mechanisms from earlier test scripts; their pinned old package/source constants are not current D08 inputs.

## Required deliverable
Clean-machine launch record including prerequisite handling and first-run behavior.

Use an existing qualifying task record when present. Otherwise the authorized record is `Build/Release/D16-03/qualification.json`, with readable explanation only when it adds required interpretation. This is task evidence, not another queue or status authority. Link original manifests/artifacts by their real paths and digests; never copy a whole package merely to rename it for this task.

## Validation contract
Packaged executable launches outside the editor, reaches real playable runtime, accepts player input, and exits cleanly; no developer-only path is required.

## Required evidence
Target-machine identity, package digest, runtime log, and captured launch/play evidence.

## Reuse boundary
Reuse D08 clean-install/play evidence only for the same exact package and a target environment independent of the development workspace. It must show the packaged executable outside the editor, real input/gameplay, prerequisites and clean exit; an editor launch or registered SDK is insufficient. Run only missing install/launch scenarios. Never reinstall already verified unchanged prerequisites, wipe the development user profile or substitute Linux/headless proof for accepted Win64 playable proof.

## Execution and closure
`docs/PRODUCTION.md` owns order/status; numeric IDs are identities, not priority. The current D-series registry and this guide are execution input; old future-task templates are historical only. Check the exact required identities, not all project history. Preserve every accepted predecessor, current worktree and raw proof. Source-lock means identity recording, not a machine/service stop.

Use deterministic file/Git/digest operations for binding and indexing. Do not create design meetings, signed-off records, approval stages, a new scheduler or an all-program closure gate. COMPLETE_ALREADY is valid only when current retained evidence actually satisfies this task; otherwise perform the missing operation and retain its real evidence. Never claim platform, player, package or publication results not observed.

The existing controller owns task advancement and per-task local/GitHub continuity. Routine Drive publication remains every five completed tasks in parts capped at 3,800,000,000 bytes. Record pending publication separately; actual external-delivery task outcomes are never replaced by local success.
