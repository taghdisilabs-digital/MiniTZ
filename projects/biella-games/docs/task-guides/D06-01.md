# D06-01 Task Guide — Cinematic and presentation integration

```yaml
schema: biella.games_task_guide/v1
task_id: D06-01
stage: 6
status: PREPARED_NEXT_TASK_INPUT
primary_contracts: [36]
```

## Objective
Integrate an in-game editable sequence into the real game world and prove clean gameplay handoff. This is runtime presentation, not a disconnected trailer scene and not new narrative canon.

## Dependency/reuse
- Reuse D05 session/pause/mode ownership rather than inventing another mode state machine.
- Reuse `BiellaGamesCharacter`, `BiellaGamesGameState`, existing camera/world actors, current production animation, VFX and audio.
- Use the real `BiellaGameplayMap` or accepted current playable world.

## Authorized project-local names
- `ABiellaCinematicDirector` — bounded owner for entering/exiting/skipping one runtime sequence and applying explicit authoritative handoff events.
- `Content/Cinematics/LS_Demo01_RuntimeHandoff.uasset` — technical in-game sequence asset.
- `BiellaCinematicTest.cpp` and `tests/run_d06_01_cinematic.py` — native/runtime acceptance fixture.

## First bounded sequence
Build a short technical sequence using existing world/player/arena-pressure presentation. It may move/cut the camera and use existing animation/VFX/audio, but it must not invent dialogue, lore, mission completion or fake combat.

Required flow:
`GAMEPLAY -> CINEMATIC MODE -> CONTROL CONSTRAINED -> EXISTING RUNTIME STATE/PRESENTATION -> WATCHED OR SKIPPED HANDOFF -> GAMEPLAY CONTROL RESTORED`.

## Acceptance checks
- Editable Level Sequence source and exact build/source identity.
- Same runtime actors/world, no duplicate fake player or disconnected scene.
- Input/camera ownership is transferred and restored.
- Watched and skipped paths produce the same required authoritative state.
- No stale input lock, missing equipment, duplicate actor or mission/world divergence.
- Streamed assets/world remain valid around the sequence.
- Capture supports presentation evidence but runtime state assertions are authoritative.
## Execution law
- This guide is prepared task input, not a second queue or completion authority. `docs/PRODUCTION.md` remains task-order authority.
- Read this guide before broad source search. Follow the cited current source/contracts; reobserve only facts that can materially change.
- Reuse verified prior work. Do not reset, clean, stash, rebuild from scratch, rename merely for aesthetics, or rerun passed evidence without material invalidation.
- The recommended project-local names below are authorized implementation names for this task. Do not pause for naming approval; change a name only when current source/compiler/API evidence requires a bounded equivalent.
- Routine dependencies, reversible environment fixes, UE authoring, test fixtures, and configured Resources are execution work, not owner-decision gates.
- Local Qwen is optional on-demand assistance. Its timeout/failure must never block the authoritative Codex turn.
- `03/04` are volatile continuity state and are not gameplay byte-identity gates.
- COMPLETE requires real editable source + task-derived runtime/package evidence appropriate to the cited contracts.

