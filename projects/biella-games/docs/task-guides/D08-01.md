# D08-01 Task Guide — Delivery, update, and release-candidate qualification

```yaml
schema: biella.games_task_guide/v1
task_id: D08-01
stage: 8
status: PREPARED_NEXT_TASK_INPUT
primary_contracts: [23, 55, 58, 59]
```

## Exact contract locations
Contract numbers identify sections inside the following files, not individual filenames. Read only the relevant sections; do not guess paths such as `docs/runtime-contracts/17*`.
- `docs/runtime-contracts/AAA_TPP_RUNTIME_CONTRACTS_21_23.md` — sections 23 (packaging, launch and play-test evidence).
- `docs/runtime-contracts/AAA_TPP_RUNTIME_CONTRACTS_50_59.md` — sections 55 (content compatibility), 58 (updates/save compatibility), 59 (release candidate).

## Objective
Produce and qualify an exact distributable build/update lineage. Release-candidate status belongs to one specific package identity, not a successful compile or screenshot.

## Accepted delivery decisions
- Primary target platform: Windows PC x64 first.
- Build/package path: UnrealBuildTool + Unreal AutomationTool; BuildGraph when useful.
- Development package for iteration/evidence; Shipping for distributable qualification.
- Runtime content: cooked Unreal assets/maps.
- Save format: UE SaveGame/custom versioned serialization at platform per-user location.

## Current resource reality
The canonical Linux UE 5.8.2 VPS has previously reported Win64 SDK as unavailable. At D08 start, reobserve current package resources. Do not silently call a Linux package the final accepted Win64 release candidate. If Win64 remains unavailable on the current Resource, classify it `REQUIRES_OTHER_RESOURCE` and continue every platform-neutral preparation/validation that does not require Win64 while routing the final package step to an actually observed compatible Resource.

## Current resource handoff
Read `Build/Release/D08-01/D08-01-resource-observation.json` before rediscovering the Windows node. This is timestamped resource evidence, not task acceptance or permanent hardware policy.
The September 8 observation found Visual Studio Build Tools and the Windows SDK already registered; no repeated installation merely because their commands are not on PATH. No Unreal installation was found in the checked registry/standard paths, no hardware GPU was exposed, and C: had 2,169,950,208 bytes free. A custom Unreal path remains UNKNOWN.
Do not spend repeated Unreal/SDK installation or launch attempts on an unchanged unsuitable Resource. Reobserve only the changed boundary or the boundary needed immediately before actual execution. Use an actually verified compatible Windows Resource for Win64 build/play qualification; continue independent package recipes, manifests, update/dependency checks and existing platform-neutral work. Do not lower quality, claim Windows delivery from Linux, or require a wholesale D07 rerun.

## Existing reusable delivery evidence
- D01-46 package checkpoint/acceptance and UAT logs under `Build/Demo01/`.
- D01-47 clean package launch evidence.
- D01-48 end-to-end packaged-runtime harness: `tests/run_d01_048.py`, `verify_d01_048.py`, `Build/Demo01/D01-048-*`.
- D07 qualification outputs become the performance/stability evidence input for this task; do not rerun D07 wholesale unless package-specific behavior differs.

## Cross-task evidence reuse
While performing required D08 work, retain the exact source/config/content and recipe identities, toolchain/environment context, actual invocation logs, package identities and package-specific install/play/persistence/update/diagnostic/telemetry results. These same original records feed D15 and D16; do not manufacture additional future-task runs.
A clean D08 reconstruction can satisfy D16-01 only when that isolation and the same material inputs are proved. D16-02 still requires two distinct actual executions of the same source/recipe; two different update versions are not equivalent evidence. D08 package-specific install, save/update and diagnostics can be reused by the corresponding D16 task when unchanged. Compare material game/build inputs rather than treating controller/document-only Git commits as game invalidation.
Reuse the existing package/launch mechanisms in `tests/cook_d03_01_package.py`, `tests/stage_d03_01_package.py`, `tests/run_d03_01_package.py`, `tests/run_d01_048.py` and `tests/verify_d01_048.py` only where applicable. Their hard-coded old package digests and source commits remain historical; bind actual D08 inputs instead of executing an old release again. Required clean-user/cache isolation does not authorize deleting the current editor caches, model caches, player saves or canonical worktree.
The resource observation is evidence for avoiding repeated unchanged failed setup, not a permanent capability ceiling. Provision or route the exact missing Windows build/play operation on an actually suitable authorized Resource; no fake resource identity, SDK reinstall loop, lower-quality platform substitution or additional approval task.

## Authorized project-local output names
- `tests/run_d08_01_release.py` — package/install/update/launch qualification orchestrator.
- `Build/Release/D08-01/source-build-manifest.json`.
- `Build/Release/D08-01/package-manifest.json`.
- `Build/Release/D08-01/qualification.json` and `qualification.md`.

## Required sequence
1. Clean dependency/content validation, including a bounded missing/incompatible-content negative case.
2. Produce exact package with source/tree/config/content identity.
3. Launch outside editor through intended entry point.
4. Exercise real input/player/camera/gameplay, rivals/infected/pressure, current world/interactions, rendering/audio/VFX, save/settings reconstruction where implemented.
5. Review fatal/assert/content-load diagnostics and package-specific resource/performance evidence.
6. For update support, prove exact old-build -> update -> launch -> supported settings/save compatibility plus incomplete/incompatible update failure handling.
7. Record known exclusions/limitations; do not invent store, launcher, certification, patch cadence or live-service requirements.

## Release-candidate acceptance
One exact packaged build must pass the current approved gameplay, production, stability and evidence contracts appropriate to its platform. Package command success alone is insufficient.
## Execution law
- This guide is prepared task input, not a second queue or completion authority. `docs/PRODUCTION.md` remains task-order authority.
- Read this guide before broad source search. Follow the cited current source/contracts; reobserve only facts that can materially change.
- Reuse verified prior work. Do not reset, clean, stash, rebuild from scratch, rename merely for aesthetics, or rerun passed evidence without material invalidation.
- The recommended project-local names below are authorized implementation names for this task. Do not pause for naming approval; change a name only when current source/compiler/API evidence requires a bounded equivalent.
- Routine dependencies, reversible environment fixes, UE authoring, test fixtures, and configured Resources are execution work, not owner-decision gates.
- Local Qwen is optional on-demand assistance. Its timeout/failure must never block the authoritative Codex turn.
- `03/04` are volatile continuity state and are not gameplay byte-identity gates.
- COMPLETE requires real editable source + task-derived runtime/package evidence appropriate to the cited contracts.

## Owner-requested installer tools
`INSTALLER_ASSEMBLY_ONLY`: use the existing capability router and `ops/workstation/INSTALLERS.md` (repository-relative). `package.installer.debian` provides `dpkg-deb` for Debian `.deb`; `package.installer.windows` provides `makensis` for Windows NSIS setup `.exe`, built locally on L40S. These wrap the correct built game payload. They do not convert a Linux build into Win64, install an OS/desktop/Steam, change game acceptance, or require a new production task/reviewer. Reuse current build/package identity and preserve saved settings; installer extraction and target runtime validation remain separate observations.

## Owner-requested Windows build setup
Use repository `ops/workstation/setup-unreal-win64.ps1`, `ops/workstation/build-unreal-win64.ps1`, and `ops/workstation/UNREAL_WIN64.md`. The setup is installed on the observed Windows node; `Build/Release/D08-01/windows-setup-observation.json` records the native compiler PASS and missing Unreal engine, not Win64 readiness. Reuse installed C++ tools; install only missing engine/tooling from actually available authorized media on adequate storage. Require the real Win64 VALID marker. Preserve native Linux package work and all accepted evidence. This setup is a required D08 resource substep, not a new task/reviewer or fabricated Windows release. The setup scripts never pause production or change task status.
