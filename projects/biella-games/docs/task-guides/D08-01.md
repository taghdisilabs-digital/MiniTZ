# D08-01 Task Guide — Delivery, update, and release-candidate qualification

```yaml
schema: biella.games_task_guide/v1
task_id: D08-01
stage: 8
status: PREPARED_NEXT_TASK_INPUT
primary_contracts: [23, 55, 58, 59]
```

## Objective
Produce and qualify an exact distributable build/update lineage. Release-candidate status belongs to one specific package identity, not a successful compile or screenshot.

## Accepted delivery decisions
- Primary target platform: Windows PC x64 first.
- Build/package path: UnrealBuildTool + Unreal AutomationTool; BuildGraph when useful.
- Development package for iteration/evidence; Shipping for distributable qualification.
- Runtime content: cooked Unreal assets/maps.
- Save format: UE SaveGame/custom versioned serialization at platform per-user location.

## Current resource reality
The canonical Linux UE 5.8.2 VPS has previously reported Win64 SDK as unavailable. At D08 start, reobserve current package resources. Do not silently call a Linux package the final accepted Win64 release candidate. If Win64 remains unavailable on the current Resource, classify it `REQUIRES_OTHER_RESOURCE` and continue every platform-neutral preparation/validation that does not require Win64 while routing the final package step to an actually observed compatible Resource.

## Existing reusable delivery evidence
- D01-46 package checkpoint/acceptance and UAT logs under `Build/Demo01/`.
- D01-47 clean package launch evidence.
- D01-48 end-to-end packaged-runtime harness: `tests/run_d01_048.py`, `verify_d01_048.py`, `Build/Demo01/D01-048-*`.
- D07 qualification outputs become the performance/stability evidence input for this task; do not rerun D07 wholesale unless package-specific behavior differs.

## Authorized project-local output names
- `tests/run_d08_01_release.py` — package/install/update/launch qualification orchestrator.
- `Build/Release/D08-01/source-build-manifest.json`.
- `Build/Release/D08-01/package-manifest.json`.
- `Build/Release/D08-01/qualification.json` and `qualification.md`.

## Required sequence
1. Clean dependency/content validation, including a bounded missing/incompatible-content negative case.
2. Produce exact package with source/tree/config/content identity.
3. Launch outside editor through intended entry point.
4. Exercise real input/player/camera/gameplay, rivals/infected/pressure, current world/interactions, rendering/audio/VFX, save/settings reconstruction where implemented.
5. Review fatal/assert/content-load diagnostics and package-specific resource/performance evidence.
6. For update support, prove exact old-build -> update -> launch -> supported settings/save compatibility plus incomplete/incompatible update failure handling.
7. Record known exclusions/limitations; do not invent store, launcher, certification, patch cadence or live-service requirements.

## Release-candidate acceptance
One exact packaged build must pass the current approved gameplay, production, stability and evidence contracts appropriate to its platform. Package command success alone is insufficient.
## Execution law
- This guide is prepared task input, not a second queue or completion authority. `docs/PRODUCTION.md` remains task-order authority.
- Read this guide before broad source search. Follow the cited current source/contracts; reobserve only facts that can materially change.
- Reuse verified prior work. Do not reset, clean, stash, rebuild from scratch, rename merely for aesthetics, or rerun passed evidence without material invalidation.
- The recommended project-local names below are authorized implementation names for this task. Do not pause for naming approval; change a name only when current source/compiler/API evidence requires a bounded equivalent.
- Routine dependencies, reversible environment fixes, UE authoring, test fixtures, and configured Resources are execution work, not owner-decision gates.
- Local Qwen is optional on-demand assistance. Its timeout/failure must never block the authoritative Codex turn.
- `03/04` are volatile continuity state and are not gameplay byte-identity gates.
- COMPLETE requires real editable source + task-derived runtime/package evidence appropriate to the cited contracts.

