# D08-01 Win64 execution handoff

Status: `PREPARED_NOT_EXECUTED`; required resource classification: `REQUIRES_OTHER_RESOURCE`.

The accepted deliverable is Windows PC x64, UE 5.8.2, DX12/SM6, with a Shipping distributable. The Linux Development archive and its tests establish only the recorded diagnostic lineage. No store, launcher, signing service or remote package destination has been selected by this handoff.

`resources/routing.json` records the actual routing result. `D08-01-resource-observation.json` records the unsuitable known Windows node. Do not reinstall its already registered Visual Studio/SDK or repeat launch/install attempts without a changed resource boundary. The smallest next action is to acquire/route to an actually compatible Windows build/play Resource, then inspect its exact Unreal installation, SDK validity, free storage and hardware GPU immediately before execution.

The newer canonical `windows-setup-observation.json` records a real compiler/link/run probe PASS at 2026-09-08 08:47 UTC and verified setup/build scripts installed at `C:\ProgramData\Biella\Win64Build`. It still reports no discovered Unreal 5.8.2, no UBT SDK validation, 3,446,833,152 bytes free and Microsoft remote/basic display adapters. The script digests match repository `ops/workstation/setup-unreal-win64.ps1` and `build-unreal-win64.ps1`; `resources/routing.json` binds that readback. Reuse this setup work. Use actually available authorized Unreal media or a preinstalled exact engine on adequate storage and a compatible GPU resource; no installer media or custom engine path has been observed here.

Use the committed Project source and `source-build-manifest.json` material-input hashes. Preserve the Linux proof. Windows builds produce their own source/build/cook/package identities and receipts; they cannot reuse Linux binary hashes or declare Linux evidence to be Windows evidence.

The exact source transfer is now prepared by `tests/prepare_d08_01_win64_source.py`. `win64-source-02/validation.json` records actual ZIP extraction and PowerShell readback of all 363 material inputs (144,638,873 source bytes), the retained source manifest, and the setup/build/verification scripts. The canonical local ZIP is `/root/biella/artifacts/games/D08-01/BiellaGames-D08-Win64-source-8c79e84957b7.zip`, SHA-256 `8b1676f4f6b8eed559d388b8ac6a9e6a602f8516d46ac8985cfddd33fe2fac85`, 88,216,176 bytes. `win64-source-02/transfer-publication.json` binds its exact committed transfer copy. This is a transfer artifact, not a second source authority or a built Windows package.

After transfer to a suitable resource, verify that ZIP digest, expand it into a **fresh** task-local directory, and run its `tooling/verify_d08_01_win64_source.ps1` with `-BundleDirectory` set to that observed directory, `-ManifestSha256 8c79e84957b78716d0de8462ac9d87e129e79a8d692c12eb548b5d93745b3660`, and `-Output` set to a fresh evidence file **outside** the extracted directory. Run this before UBT/UAT creates build outputs. The verifier checks exact membership and bytes, compatible engine requirement, and absence of build output/cache/player state. Its PowerShell 7.6.5 execution on Linux passed clean readback, missing-map and corrupt-map rejection, incompatible engine-requirement rejection, unexpected cache-directory rejection, and exact restoration. No Windows execution, engine availability, clean rebuild or reproducibility result is inferred. `win64-source-01/validation.json` retains the initial path-order comparison failure; membership was unchanged and the comparison was repaired without changing any game inputs.

The extracted `project/BiellaGames.uproject` and sibling `tooling` scripts are the inputs to the retained build operation below. They contain no engine or SDK. Do not run the Linux build/source-manifest helper as if it generated Win64 identities. Windows UBT/UAT and native qualification must still record their own material inputs, exact command/toolchain, binary/cook/package receipts, and actual play/update evidence. The Development settings probe adaptation described below remains required before that diagnostic scenario can run on Windows.

The following PowerShell command forms use explicit paths supplied by the observed Resource. `$EngineRoot` is the directory containing `Engine`; `$ProjectFile` is the exact editable `BiellaGames.uproject`; `$ArchiveDirectory` is a fresh task-scoped local output directory on that Resource. These are inputs, not claims that paths/resources exist. Validate `Engine/Build/Build.version` as 5.8.2 first.

```powershell
& "$EngineRoot\Engine\Build\BatchFiles\Build.bat" -Mode=ValidatePlatforms -Platforms=Win64 -OutputSDKs
```

Require the actual `##PlatformValidate: Win64 VALID` marker with its SDK version. The locally inspected UE `ValidatePlatformsMode.cs` returns zero even for INVALID, so exit code alone is insufficient.

```powershell
& "$EngineRoot\Engine\Build\BatchFiles\RunUAT.bat" BuildCookRun `
  "-project=$ProjectFile" -noP4 -unattended -platform=Win64 `
  -clientconfig=Development -build -cook -stage -pak -iostore `
  -archive "-archivedirectory=$ArchiveDirectory"
```

Retain full UBT/UAT diagnostics and exact editor/game receipts, cooked file set, source/config/content hashes and archive extraction readback. Use the resulting Development package for native diagnostic evidence. Build a separate fresh Shipping archive with `-clientconfig=Shipping` and its own identities; the Development automation probe is intentionally excluded from Shipping.

Launch the actual packaged Windows entry point outside the editor on a clean per-user state with source/DDC absent. Observe actual DX12/SM6, input/player/camera, gameplay state, rivals/infected/pressure, current environment interactions, audio/VFX, UI and watched/skipped cinematic handoff, process teardown, content diagnostics and frame/resource evidence. Shipping acceptance requires real play evidence on its exact bytes; a Development automation pass or compile cannot qualify Shipping.

For the Windows update lineage, preserve an actual runnable old Windows package and its runtime-created per-user settings, verify an exact full update, launch the new package, and prove supported settings reconstruction across separate native processes. Exercise incomplete/mixed/incompatible update rejection while retaining the old runnable version. Do not install the Linux update base on Windows. There is currently no world SaveGame implementation to claim as migrated; arbitrary future schema compatibility is unproved.

The portable Python installer in `tests/run_d08_01_release.py` validates byte membership and PE/ELF platform identity and retains old versions before atomic activation. Its Windows locking/path and flush branches now have actual native fixture execution in `tool-tests/windows-update-02/native-validation.json`: all 14 tests passed, including incomplete/mixed/incompatible update rejection, interrupted-copy/pointer recovery, concurrent-lock rejection, and installed x64 fixture launch before and after the update. `tests/run_d08_01_windows_update_tests.py` reuses the existing fault suite with real x64 PE fixture bytes and explicitly synthetic content. It does not prove cooked game content, game play, or UE settings/save migration. `tool-tests/windows-update-01/` preserves the discovered Windows read-only-handle `fsync` failure; the repair opens only the private Windows copied files read/write before flushing. The Linux branch is unchanged and all 13 affected regression tests passed in `tool-tests/windows-update-linux-regression-01/`.

The native launch driver and `verify_d08_01_release.py` remain explicitly Linux diagnostic tooling. The D08 Development settings probe also checks the Linux sandbox's `/tmp/state/user/` path; adapt that assertion to the actually isolated Windows per-user path along with the native driver. Preserve feature checks without making diagnostic automation a Shipping prerequisite.

The configured `package.installer.windows` capability supplies NSIS (`makensis -V2`). The editable `tests/build_d08_01_windows_installer.py` now generates the task-local `.nsi` from an exact Win64 manifest, rejects Linux bytes, and verifies the setup PE format and complete extracted payload digests. It installs each identity under its own per-user package directory, rejects an existing version without overwriting it, and generates an uninstaller with explicit file membership. It does not install UE prerequisites, alter saves/settings, configure a store, or replace the portable D08 update qualifier. Its embedded-data limit is 1.8 billion bytes; larger actual payloads require a separately qualified external-data recipe rather than truncation.

`tool-tests/nsis-04/validation.json` records successful assembly/extraction of two explicitly labeled **INSTALLER_TEST_FIXTURE_NOT_GAME** payloads, including native x64 PE format, Unicode/dollar-sign paths, and rejection of Linux input, corrupted bytes, altered identity and fixture input without opt-in. The small fixture was built with the existing local LLVM tools; this is not Unreal compilation and did not repeat the Windows compiler setup. The NSIS wrapper is PE32 x86; its contained fixture is PE32+ x64. Neither fixture is a game package or an update compatibility result.

Native installer install/launch/uninstall checks in `tests/test_d08_01_windows_installer.ps1`, bound by the unchanged `tool-tests/nsis-04/native-plan.json`, now **PASS** on `WIN-P7MA35DRF6Q`. `tool-tests/nsis-04/native-01/` retains the seven native checks, exact remote report bytes and SHA-256 readback. Both setup artifacts and the script matched their prepared identities before execution at `C:\ProgramData\Biella\Win64Build\D08-nsis-native-01`. First installation, native x64 fixture execution, existing-version rejection, side-by-side identity preservation, and both uninstall paths passed. The deliberately unowned file and external fixture-state sentinel survived; no fixture process remained. Only fixture state was used, never real player saves. The prior HTTP 504 failures remain historical evidence. This result cannot qualify the missing game payload and does not require another fixture run.

`resources/windows-route-review.json` retains the current empty Win64 route, official Modal/Saturn image-capability findings and connector failures. No newly compatible Windows build/play Resource was observed. Preserve the original Linux diagnostic qualification and its resource receipt: the new review supplements it without changing those accepted bytes. The Linux diagnostic `.deb` uses `tests/build_d08_01_installer.py` and does not satisfy the Windows criterion.

`resources/windows-offline-observation.json` preserves the earlier offline observation. `resources/windows-native-observation.json` records recovered connectivity, actual fixture execution, and Microsoft remote/basic display adapters. The subsequent `resources/windows-media-observation.json` binds the original lookup receipts in `resources/windows-media-readback.json`: at 10:55 UTC, C: had 4,155,490,304 bytes free; G: was identified as the Google Drive FAT32 mount, not a separate independent build disk. Direct-child name checks in `C:\tmp`, the Administrator profile and Downloads found no Unreal/Epic/UE5 media candidates. Arbitrary nested/custom engine paths remain UNKNOWN. No SDK or engine installation was repeated.

The metadata lookup verified `GAMES/03_CONTEXT/unreal` in Drive as folder `1iMu5XD217l2a0VT1sspWx65pP5vkkTYa`; its only returned children were `RUNTIME.md` and `RENDERING_PERFORMANCE.md`. No engine media was found in this bounded scope. The existing `gh` identity could not resolve `EpicGames/UnrealEngine`, and the configured GitHub connector returned HTTP 404 for its `Engine/Build/Build.version`. These results establish unavailable source access through the checked paths, not repository absence or an entitlement diagnosis.

The Win64 resource route is still empty. [AWS's official Unreal workstation guidance](https://aws.amazon.com/blogs/gametech/getting-started-with-unreal-engine-development-on-aws/) describes a possible GPU provider, but no AWS CLI, authentication environment binding or standard credentials/config file was present in this execution environment. The linked Marketplace page did not establish an exact UE 5.8.2 image. [Epic's offline installer documentation](https://dev.epicgames.com/documentation/en-us/unreal-engine/offline-installer-of-unreal-engine) describes organization-controlled downloads; no matching accessible installer was observed. No cloud resource was provisioned. Revisit only a supplied/changed engine path, media identity, connected resource or callable authenticated provisioning boundary, rather than repeating the same searches.

Source-transfer preparation, native NSIS fixture lifecycle, and native portable-installer fault validation are complete within their stated scopes. Do not repeat them, Linux qualification, or unchanged SDK setup on another task resume. The next operation needs a concrete external binding: a connected Windows resource and exact Unreal 5.8.2 engine root with adequate storage and hardware DX12 SM6 GPU, or a callable authenticated provisioning path plus matching authorized engine media. Once that binding exists, verify the retained transfer and actual Win64 SDK marker, then perform the native build/play/update handoff above. Authorization for useful provisioning already exists; the missing resource/media/authentication facts do not create a new approval task.

Publish only to the canonical destination actually supplied by Project/task authority, with exact remote identity and readback. Auto Feeder owns routine GitHub/Drive continuity publication from exact committed task bytes. No remote delivery is claimed here.
