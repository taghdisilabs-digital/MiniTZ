# D03-01 package qualification execution note

Contract: docs/runtime-contracts/AAA_TPP_RUNTIME_CONTRACTS_40_49.md, 46 and 49. This note is implementation evidence, not task authority.

1. Incrementally build the current Linux Development game target; hash source before and after and retain the native log.
2. Snapshot exact current Source/Content/Config/project and matching binaries into a task-scoped staging directory. Cook current playable maps into that snapshot with an isolated file-system DDC graph; preserve the ordinary editor caches and previous packages.
3. Stage and archive exact cooked bytes. Qualify the resulting Development package outside the editable project, with isolated user/cache directories, through the existing D03 surfaces and D02 environment native tests on TSR and NativeTAA.
4. Record shader-library loading, pipeline-cache preparation, creation timing and cold/warm cache identities from actual engine output. Test missing shader data using a separate damaged copy only when exact package structure supports a bounded negative control.
5. Hash package, source, logs and runtime readback; inspect representative captures, record limits, commit project implementation/evidence locally. Auto Feeder owns remote publication and canonical state transitions. D03 stays open for other presentation requirements.

No reset, cache deletion, new queue, shipping-target change, or subagent workflow. All failure attempts retained.
