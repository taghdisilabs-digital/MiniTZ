# D03-01 — Cosmetic ground support in the playable world

Task status: **CONTINUE**. A filtered PBR ground surface now fills the tested black margins beside the street and beneath the existing architecture. This is a bounded presentation increment; a thin distant boundary and the broader unfinished world art remain visible.

`SourceAssets/Architecture/ground-support.json` is the editable recipe. `Content/Python/author_ground_support.py` authors and independently reads back one nonspatial, HLOD-excluded actor and its material instance in the existing World Partition map. The 1,200 × 1,000 metre surface ends 8 cm below the existing street. It has no collision, navigation influence or cast shadow. Gameplay capsules, route geometry and the existing street collision remain authoritative. The assets and captures remain `GENERATED_DRAFT`.

The initial strict authoring gate failed because saving the new external actor also resaved the main map. That failure is retained. `ground-map-audit-01/verification.json` separately verifies the exact 13,330-byte before/after map copies and the bounded changed offsets. Native post-load text exports differ only in regenerated persistent-partition names/references/debug colour; all other exported lines match in order. The engine implementation explains those regenerated objects. All 219 other pre-existing assets remain byte-exact, with 66 existing actor invariants unchanged. Five corrupted export controls reject altered loading range, client visibility, partition class, an extra property and an unrelated world property. This exception does not allow arbitrary map or World Partition changes.

`ground-build-04` passed the incremental native editor build from exact source. `ground-readback-02` passed without modifying any assets. `ground-tsr-01` and `ground-taa-01` passed the same native surface scenario under SM6/TSR and SM5/TAA. `ground-environment-01` passed the affected environment/traversal regression. The hidden-ground control preserves the same camera sequence and exits cleanly with its deliberate native visibility assertion registered; the external gate rejects it solely for ground visibility. Earlier module, permission, map-save and negative-shutdown failures remain as raw evidence.

`ground-verification-01.json` verifies the shared 355 runtime input identities, exact native source/build lineage, 43 image identities for each surface run, and ten matched static camera poses. Forty pixel checks cover two world-only margin rectangles. Hidden-ground pixels are over 90% black; both visible profiles are below 1%, with mean brightness gain over 30/255. The existing street comparison differs by less than 2/255 mean absolute RGB. These thresholds qualify those margins, not the whole scene. Nine exact PNGs and their digest manifest are retained at `/root/biella/artifacts/games/D03-01/ground-visual-review-01/`.

The native variable-time, uncapped 1280×720 editor runs use existing DDC and no generated frames. Static/pan frame-time medians are approximately 13.86/14.81 ms for TSR and 13.26/13.69 ms for TAA; p95 values are approximately 16.07/16.49 and 14.95/15.08 ms respectively. These are scoped observations, not a final performance budget or isolated causal cost measurement. Gross capture stability and direct inspection do not establish fine temporal quality.

The loading-screen module now explicitly depends on CoreUObject, correcting an editor link failure hidden by the monolithic game build. Its exact bytes are verified at build and aggregate readback, and runtime logs confirm loading. The older runtime harness did not hash this module before/after each run; that limitation is explicit. The main gameplay editor module and native source do have matching run/build identity records.

Two bounded optional local-Qwen reviews timed out. Their prompts, output and receipts are preserved; no provider verdict is claimed. The direct source review and negative controls are recorded in `ground-review-disposition.json` and the map-audit directory. Prior local assistance informed only the bounded cosmetic approach.

The new assets still require a fresh isolated cook and packaged runtime qualification. Broader world/lighting/art, additional VFX/audio coverage, uninterrupted cold startup motion and finer temporal/native performance evidence remain open. Canonical local bytes are preserved; Auto Feeder owns GitHub/Drive publication and task transition. This increment makes no remote-publication or D03-completion claim and changes no task-order/status authority.
