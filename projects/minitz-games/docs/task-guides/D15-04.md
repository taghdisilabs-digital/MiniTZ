# D15-04 Task Guide — Bind production-ready predecessor build

```yaml
schema: biella.games_task_guide/v1
task_id: D15-04
project: Biella Games
status: PREPARED_TASK_INPUT
priority: GAME_FIRST
execution_root: projects/minitz-games
```

## Objective
Bind the polished D17 source identity and the most recent matching package/build lineage. Reuse D08 packaging mechanics; do not pretend a pre-visual D08 package represents changed D17 source.

## Exact inputs
- `Build/AAA/D17-08/qualification.json`
- `Build/Release/D08-01/package-manifest.json`
- `Build/Release/D08-01/qualification.json`

D08 output paths are reusable delivery-mechanism evidence; D17-08 is the authoritative polished-game predecessor after this priority amendment. Resolve them after accepted D08 closure. If a required record is missing, repair that exact handoff rather than searching all Drive/history or fabricating it. Reuse mechanisms from earlier test scripts; their pinned old package/source constants are not current D08 inputs.

## Required deliverable
Build identity record with source commit/tree, package path or artifact identity, digest, configuration, platform, and prior release-qualification evidence.

Use an existing qualifying task record when present. Otherwise the authorized record is `Build/Release/D15-04/qualification.json`, with readable explanation only when it adds required interpretation. This is task evidence, not another queue or status authority. Link original manifests/artifacts by their real paths and digests; never copy a whole package merely to rename it for this task.

## Validation contract
Exact retained package and source identities match the accepted game release. Repair only a missing/corrupt boundary from preserved source; do not replay valid build work or stop for unrelated program completion.

## Required evidence
Exact package digest, source identity, and predecessor validation references.

## Reuse boundary
Resolve the accepted package through the D08 package manifest and qualification, not a stale task filename or current HEAD guess. Compare exact package/source/configuration/platform identities. Preserve original package bytes. Missing metadata is repaired locally; an actual missing/corrupt package invalidates only that artifact boundary, not completed gameplay. This does not freeze production: no build or runtime rerun for valid retained bytes. Use the publication cursor, not a new remote wait.

## Execution and closure
`docs/PRODUCTION.md` owns order/status; numeric IDs are identities, not priority. The current D-series registry and this guide are execution input; old future-task templates are historical only. Check the exact required identities, not all project history. Preserve every accepted predecessor, current worktree and raw proof. Source-lock means identity recording, not a machine/service stop.

Use deterministic file/Git/digest operations for binding and indexing. Do not create design meetings, signed-off records, approval stages, a new scheduler or an all-program closure gate. COMPLETE_ALREADY is valid only when current retained evidence actually satisfies this task; otherwise perform the missing operation and retain its real evidence. Never claim platform, player, package or publication results not observed.

The existing controller owns task advancement and per-task local/GitHub continuity. Routine Drive publication remains every five completed tasks in parts capped at 3,800,000,000 bytes. Record pending publication separately; actual external-delivery task outcomes are never replaced by local success.
