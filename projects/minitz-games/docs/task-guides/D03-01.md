# D03-01 Task Guide — Production rendering, animation, VFX, and audio quality

```yaml
schema: biella.games_task_guide/v1
task_id: D03-01
stage: 3
status: CURRENT_RUNNING_INPUT
primary_contracts: [04, 14, 16, 20, 21, 37, 39, 46, 49]
```

## Objective
Raise production presentation on the existing playable world. Do not create disconnected showcase scenes. Gameplay, rendering, animation, VFX and audio must remain driven by the existing authoritative runtime.

## Already verified — reuse, do not redo
- Rendering checkpoint: `Build/Presentation/D03-01-progress.md` and `D03-01-rendering-increment.json`. Filtered PBR world surfaces, TSR/TAA runtime profiles, environment regression and negative controls passed.
- Animation checkpoint: `Build/Presentation/D03-01-animation-progress.md` and `D03-01-animation-increment.json`. Shared skeletal runtime locomotion/jump/lifecycle/readability passed.
- Audio/VFX checkpoint: `Build/Presentation/D03-01-audio-progress.md`, commit `1898b568bf6c6dceae9dcf6aa66ef7a45a6f6e1d`. Critical-cue ducking, 12-phase feedback regression and six negative controls passed.
- Terrain foot-contact correction is verified and reusable. `Build/Presentation/D03-01-contact-progress.md` records the native positive scenario, disabled negative control, animation regression, preservation manifest, and final readback. Do not reopen it without material invalidation.
- Aim/action/hit/defeat, skeletal vehicle/seat, reconstruction, SM5/SM6 packaging, handoff, automatic-PSO observation and cosmetic ground/package increments are also preserved in their existing D03 progress/readback records. Reuse them; do not regress to an older frontier.
- Current frontier is the portable PSO seed + native loading-display cold-start qualification under `Build/Presentation/pso-loading-*`, with the exact retrieval index at `/root/biella/artifacts/games/D03-01/D03-01-qualification-retrieval-index.json` and owner support guide at `/root/biella/artifacts/games/D03-01/D03-01-owner-support-guide-01.json`.

## Immediate continuation
1. Start from `Build/Presentation/pso-loading-empty-01/validation.json`, `Build/Presentation/pso-loading-seeded-01/validation.json`, and their exact runtime logs. Preserve the current dirty PSO/loading work and recorded input hashes; do not recreate passed contact/animation/package evidence.
2. Keep the existing cold-start acceptance unchanged: no native fatal/crash, exact lineage and handoff checks preserved, no unmeasured loading frames, and sampled loading-display longest static interval **<1.0 s**. Do not relax this threshold or treat diagnostic controls as production acceptance.
3. Treat the seeded failure precisely: the 68 seeded file-cache precompile tasks completed before `LoadMap`; do not claim they directly caused the later crash. The observed later boundary is the installed UE 5.8.2 `FinishDestroy` timeout path after >10 s with non-ready destruction work while other pipeline activity remained possible.
4. Use one matched diagnostic seeded run with `gc.MaxTimeForFinishDestroyGC=40` only through the harness's existing DPCVars mechanism if still needed to classify timeout/starvation. Verify native config readback. Never ship/qualify the override and never count a longer timeout as PASS.
5. Independently isolate the 2.0 s displayed-static interval. The loading widget uses continuous `FPlatformTime::Seconds()` phase; distinguish indicator paint/geometry progression from render submission and actual presented/display frames. A loader render gap <=0.410917 s is not proof of displayed motion.
6. Make only the smallest source/test change supported by that evidence, then run the exact affected positive/control diagnostic. Preserve failed hypotheses and exact identities. If the portable seed path is rejected, keep its evidence and pursue another supported solution for the same cold-start/display-motion gate; do not skip the gate.
7. After the cold-start frontier is resolved, continue only the remaining open D03 qualification gaps from current progress/retrieval evidence: world/horizon production art, broader VFX/audio coverage, fine native temporal/performance evidence, and final owner visual acceptance.

## Existing entry points
- Animation: `Source/BiellaGames/Private/BiellaCharacterAnimInstance.cpp`, `Public/BiellaCharacterAnimInstance.h`, `Private/Tests/BiellaAnimationTest.cpp`.
- Gameplay feedback: `BiellaGameplayFeedback.*`, `BiellaAudioMixTest.cpp`, `BiellaFeedbackTest.cpp`.
- Rendering authoring: `SourceAssets/Materials/ProductionSurface.hlsl`, `Content/Python/build_production_surfaces.py`.
- World/runtime regressions: `tests/run_d02_04.py`, `tests/run_d03_01_surfaces.py`, `tests/run_d03_01_animation.py`, `tests/run_d03_01_audio_mix.py`.

## Accepted rendering/runtime baseline
UE 5.8.2; C++ authoritative gameplay; Deferred + Nanite + Lumen + VSM; TSR vendor-neutral baseline; Niagara; Unreal Audio Mixer + MetaSounds. Optional vendor features are replaceable and do not define correctness.

## Evidence required before D03 closure
- Native build from exact source.
- Interactive/runtime proof for production animation, VFX/audio and representative rendering from the same source/build lineage.
- Motion/temporal stability and measured native frame-time evidence; generated frames reported separately if used.
- Production asset traceability and representative runtime integration.
- Shader/material/PSO behavior not dependent on hidden editor cache.
- Supported fallback path preserves gameplay visibility.
## Execution law
- This guide is prepared task input, not a second queue or completion authority. `docs/PRODUCTION.md` remains task-order authority.
- Read this guide before broad source search. Follow the cited current source/contracts; reobserve only facts that can materially change.
- Reuse verified prior work. Do not reset, clean, stash, rebuild from scratch, rename merely for aesthetics, or rerun passed evidence without material invalidation.
- The recommended project-local names below are authorized implementation names for this task. Do not pause for naming approval; change a name only when current source/compiler/API evidence requires a bounded equivalent.
- Routine dependencies, reversible environment fixes, UE authoring, test fixtures, and configured Resources are execution work, not owner-decision gates.
- Local Qwen is optional on-demand assistance. Its timeout/failure must never block the authoritative Codex turn.
- `03/04` are volatile continuity state and are not gameplay byte-identity gates.
- COMPLETE requires real editable source + task-derived runtime/package evidence appropriate to the cited contracts.

