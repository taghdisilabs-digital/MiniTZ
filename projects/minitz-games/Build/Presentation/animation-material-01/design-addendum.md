The first 720p/1080p native readability regression passed structural assertions,
but direct capture review exposed an excessively dark player in the shadowed
D01 fixture. Retain that failed visual candidate. Derive a copy of the imported
mannequin material and its two instances; add 0.035 times the existing textured
base color to the existing emissive output. Preserve all other graph inputs,
textures, instance overrides, source assets and lit response. This is bounded
character presentation, not a substitute for unfinished world lighting.

Verify the saved graph and inherited texture identities in a fresh Unreal
process, assert bounded fill on actual runtime slots, then compare the player
region in native before/after captures at both existing viewport sizes. Repeat
the animation and environment regressions against the final runtime bytes.
