Bounded UE 5.8.2 C++ review. No task authority or acceptance. Current game loading screen exits when engine loading finished and PipelineStateCache::GetNumActivePipelinePrecompileTasks()==0. Installed PipelineStateCache.cpp shows this counter is incremented/decremented ONLY when Initializer.bFromPSOFileCache. Runtime: Vulkan PSO Precaching=1, PipelineFileCache=0; loading stop pending=0, then first world compute PSO hitches status Too Late. NumActivePrecacheRequests() separately returns graphics + compute active precaches (atomic), guarded by IsPSOPrecachingEnabled. It honors max(TaskPriorityThreshold,MinPriorityForActivePrecacheRequests), both default0. Plan: shared snapshot of BOTH counters; loader and cosmetic first-world overlay wait for both, retain file-cache counter too, record counters and priority cvars in native telemetry. Same-binary test flag bypasses only auto wait. Engine source not changed; do not change scheduling or retention. Identify at most 3 concrete concurrency/lifecycle caveats or meaningful negative-control checks. Do not invent APIs.