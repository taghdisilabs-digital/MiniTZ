{
  "prompt": "Non-authoritative bounded Unreal audio diagnosis. Current native fixture uses -game -DeterministicAudio -UseFixedTimeStep -FPS=60. Real gameplay shot and impact UAudioComponents exist at authored gains. UI pressure wave records nonzero. Combat-only recorded WAV is entirely zero, mixed WAV seems only pressure. Installed UE 5.8.2 NonRealtimeAudioRenderer snapshots TickDelta = FApp::GetDeltaTime() at InitializeHardware and calls RenderAudio(TickDelta) each OnHardwareUpdate. Source WAVs ForceInline PCM. All sounds finite, feedback prunes when !IsPlaying or world age exceeds duration/pitch+0.25. Recording spans 1.55 world seconds but files have only 0.32 to 0.66 seconds of PCM. Original broader event feedback test passed but also emits nonspatial player hurt. Give 3 prioritized concrete hypotheses and the minimal native observation to discriminate. Do not invent APIs or suggest disabling spatial audio for acceptance."
}
