Native RED-03 passes exactly three expected missing-duck assertions, with no other automation error. Spatial components remain live at normal gains and the listener is within 1,100 cm. Yet combat PCM is zero. NRT snapshot TickDelta differs from world simulation duration; the local Qwen response invents an audio clock rounding mechanism absent from installed source and is not adopted. Test the existing vehicle runner transport (SDL dummy device, ordinary AudioMixer) while retaining native spatialization, decoding, sources and all fixture assertions. This changes capture transport only.

## GREEN-01 waveform alignment repair

Native assertions, clean shutdown, and unchanged input/authority hashes passed. The original independent correlation chose combat lag -87 samples because the louder critical cue dominated it. Subtracting the fitted critical reference before aligning combat selects lag 0 without using the expected gain. The unchanged raw recording fits combat 0.2437736714, critical 0.9999146942, residual 0.016707591. RED-05 remains combat 0.9752695422, critical 0.9980386176, residual 0.0607499155. Preserve original failing validation and verifier; the fresh run uses this corrected estimator with all prior thresholds unchanged.
